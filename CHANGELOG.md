# 更新日志

## v2.7.0 (2026-02-12)
### 新增
- **AI笔记问答功能**：在笔记功能中集成AI问答能力
  - 知识按钮三态切换：全部列表 → 知识列表 → AI列表
  - 聊天式交互：独立输入栏 + 只读滚动对话内容
  - 支持OpenAI兼容接口的SSE流式响应
  - 多轮对话：同一AI笔记支持连续提问
  - 大窗口AI模式：只读对话区 + 底部输入框
  - 对话内容自动滚动到底部
  - 操作按钮（发送/复制/删除）始终显示
- **悬浮球绑定AI列表**：右键菜单新增"AI列表"绑定选项，点击直接打开AI对话列表（含完整工具栏）
- **AI聊天复制到知识库**：AI聊天列表右键菜单新增「复制到知识库」子菜单，支持选择知识库分组创建副本
- **笔记置顶功能**：笔记右键菜单新增「移到最顶端」/「取消置顶」选项
  - 新增 pinnedAt 字段，置顶笔记始终排在列表最前面
  - 多个置顶笔记按置顶时间倒序排列
  - 支持普通笔记、知识库笔记和 AI 聊天笔记
  - 数据库自动迁移，重启后置顶状态持久化
- **悬浮球分组前移后移**：分组悬浮球右键菜单新增「前移」「后移」选项，调整组内相对位置
- **AI内容Markdown渲染**：AI聊天内容支持Markdown格式渲染
  - 代码块、标题、列表、粗体、斜体、引用、链接等完整Markdown支持
  - Mermaid流程图渲染（通过WKWebView + mermaid.js）
  - 流式传输中用纯文本显示，完成后切换Markdown富文本
  - 自定义暗色主题适配深色界面

### 优化
- **大窗口背景完全不透明**：笔记和AI大窗口背景从半透明改为完全不透明
- **AI列表加载速度优化**：列表移除Markdown渲染，纯文本显示，hover提示双击大窗口查看
- **AI笔记颜色优化**：新建AI笔记颜色限定为蓝、紫、深色，不再出现橙色
- **悬浮球外发光浓度加深**：glowIntensity默认值从0.8提升至1.0
- **分组悬浮球间距调整**：分组内悬浮球间距改为-10，实现轻微重叠效果

### 新增
- **复制到知识库Toast通知**：AI笔记右键复制到知识库后显示确认提示

### 修复
- **Opencode ACP连接管理**：修复第二次发送卡顿问题，添加进程存活检查、5分钟空闲超时重建、请求超时机制、管道写入保护

### 修复
- **分组悬浮球颜色修改无效**：修复分组中单独修改悬浮球颜色不生效的问题
- **视频模式退出失败**：修复CGEventTap被系统超时禁用后键鼠事件不再接收的问题

- **opencode ACP 协议集成**：将 opencode 后端从 CLI 模式升级为 ACP（Agent Client Protocol）协议
  - 真正的逐 token 流式输出（agent_message_chunk 事件）
  - 手动 JSON-RPC 2.0 over stdio 实现，零第三方依赖
  - opencode acp 进程常驻，响应更快
  - 多轮对话天然上下文保持
  - 延迟完成机制解决 prompt 响应先于 chunks 的时序问题
- **AI发送loading白色**：AI聊天发送按钮和大窗口标题栏的 ProgressView loading 指示器颜色改为白色
- **opencode 模型动态查询**：设置中 opencode 模型选择改为 Picker 下拉菜单，自动通过 CLI 查询可用免费模型
- **ACP 服务控制面板**：在设置中新增 ACP 服务管理
  - 手动启动/停止 ACP 服务按钮
  - 实时连接状态显示（未连接/连接中/已连接）
  - 应用启动时自动启动 ACP 服务选项

### 修复
- **盒盖唤醒无蒙版**：修复合盖休息充足时间后打开盖子，未显示"休息完成"蒙版的问题
- **蒙版残留**：修复休眠唤醒后蒙版未关闭、活动检测回调未恢复的问题
- **快捷键隐藏悬浮球需按两次**：修复笔记窗口打开时按快捷键需按两次才能隐藏的问题

### 重构
- **智能提醒系统重构**：完全按需求重写智能提醒核心逻辑
  - 6状态状态机：working/idle/videoMode/away/rested/overlay
  - 蒙版带休息倒计时，时间到后自动关闭
  - 代码拆分：SmartReminderManager.swift (297行) + Extensions (160行)
  - ReminderOverlayWindow 精简到194行
- **通知去重**：修复锁屏/解锁通知双重触发导致回调重复执行的问题
- **状态持久化**：补充保存 isWorkPaused、pausedActiveDuration 等关键字段，恢复时智能重建状态

## v2.6.10 (2026-02-12)
### 修复
- **大窗口编辑笔记未保存**：修复了在大窗口编辑笔记后关闭窗口，双击笔记进入行内编辑时内容还原的问题。根因为 `NoteItemView` 的 `@State content` 未与外部数据变更同步。

### 优化
- **编辑光标定位**：双击笔记进入行内编辑时，光标自动定位到文本末尾，更符合追加编辑的使用习惯。

### 新增
- **AI 提供商预设**：AI 设置页面新增预设选择器，支持一键切换 OpenAI / DeepSeek，自动填充接口地址和模型名称。

### 优化（大窗口编辑）
- **背景颜色**：大窗口背景色与笔记卡片颜色一致
- **窗口定位**：大窗口显示在笔记列表右侧，避免遮盖
- **视觉细节**：文字白色、关闭按钮白色、隐藏滚动条、弹簧动画出现

## v2.6.9 (2026-02-11)
### 新增
- **数据备份功能**：实现了完整的数据备份与恢复系统
  - 支持手动备份和自动定时备份（每天/每周/每月）
  - 备份内容包括所有笔记、分组和应用配置
  - 可自定义备份路径和最大备份数量
  - 支持从历史备份恢复数据
  - 备份文件采用 SQLite + JSON 混合格式，包含完整的备份清单
  - 集成到设置页面，提供友好的备份管理界面

### 优化
- **构建脚本增强**：优化 manage_app.sh 构建脚本
  - release 模式现在支持自动生成和嵌入应用图标
  - 打包命令支持智能版本号管理，自动读取并递增版本号
  - 简化开发和发布流程
- **透明度设置迁移**：将笔记、工具栏、分组透明度设置由隐藏项迁移至"通用"配置页，提升设置可达性。
- **配置界面精简**：清理了隐藏设置中的冗余项（如全局快捷键）和废弃项（如呼吸效果配置），并移除了插件设置中的按钮绑定逻辑，提升交互清晰度。
- **视觉真实度提升**：移除透明度硬编码折减系数，确保 100% 透明度设置值在视觉上呈现完全不透明效果。
- **悬浮球瘦身**：移除使用频率较低的悬浮球"呼吸灯"动态效果及相关透明度配置项，进一步降低能耗。
- **悬浮球分组逻辑**：移除了默认生成的"分组1/2/3"，初始状态更加纯净；Demo 初始化创建的悬浮球默认不再进入任何分组。
- **分组设置体验**：优化个性化设置面板，在选择分组时，尺寸、形状和排列方式将即刻同步为该分组成员的配置。
- **默认配置调整**：笔记窗口默认高度调整为屏幕高度的 60%（五分之三），优化初始显示体验。
- **笔记视觉微调**：笔记列表底部增加智能磨砂渐隐遮罩，精确适配笔记宽度与圆角，消除滚动截断感。
- **智能提醒优化**：去除了工作计时中的系统音频检测逻辑，现在空闲状态判定将完全基于真实的物理输入（键盘、鼠标），逻辑更清晰且能进一步降低系统开销。
- **智能提醒空闲阈值调整**：将默认空闲阈值从 120 秒降低到 60 秒，使系统能更快速地识别用户离开，并自动进入休息状态，提升响应灵敏度和用户体验。
- **全局快捷键设置交互优化**：快捷键输入框默认禁止编辑，点击"修改"按钮后才能编辑，保存后自动恢复禁用状态，防止误操作并提升设置安全性。
- **音频检测时机优化**：将音频检测启动时机从键鼠停止后立即开始延迟到 15 秒后，避免短暂停顿（如思考、阅读）时误判为看视频状态，提升空闲检测准确性。
- **快捷键默认值优化**：将"显示/隐藏悬浮球"快捷键默认值从 Control+Shift+Cmd+O 改为 Control+Shift+Cmd+X，更符合"关闭/隐藏"的语义联想。同时在快捷键设置页面添加修饰键快速复制按钮（Control、Shift、Cmd、Option），用户点击即可复制到剪切板，简化快捷键输入流程。

### 修复
- **笔记列表滚动**:修复了鼠标位于笔记列表项间隙时滚动失效的问题。
- **取色器菜单快捷键显示**：状态栏"取色器"菜单项的快捷键显示与偏好设置保持一致。
- **笔记列表圆角遮盖**：修复了笔记列表容器底部圆角未能完全遮盖笔记项的问题，通过移除硬编码遮罩并应用层级圆角实现精确剪裁。
- **笔记颜色稳定性**：优化了笔记颜色存储逻辑，将颜色字段设为强制持久化项，修复了移动笔记或重启应用时可能导致的随机变色问题。
- **休息状态菜单栏时间更新频率**：修复了休息状态下菜单栏时间大约5秒才更新一次的问题，移除StatusBarTimerView的内部定时器，改为完全依赖SmartReminderManager的通知驱动，实现每秒稳定更新。
- **并发安全性修复**：修复了 ReminderOverlayWindow 系统唤醒监听器中的 Swift 并发警告，将 main actor-isolated 属性访问移入 MainActor 上下文，确保线程安全。
- **系统唤醒后工作倒计时恢复**：修复了系统休眠唤醒后，虽然空闲计时正常更新但工作倒计时停止更新的问题。唤醒时现在会检测并恢复工作暂停状态（isWorkPaused），确保工作计时能继续累加，同时增加了关键节点的日志输出便于追踪。
- **蒙版关闭后焦点冻结**：修复了休息提醒蒙版关闭后应用无响应的问题（无法滚动、无法聚焦输入）。蒙版关闭时现在会自动恢复焦点到偏好设置窗口或其他可见窗口，确保应用保持响应状态。


## v2.6.8 (2026-02-10)

### 新增
- **分组合动功能**：属于同一分组的悬浮球现在支持大小与形状同步。修改组内任意悬浮球的大小或形状，其他成员将实时跟随变化（颜色仍保持独立）。
- **个性化形状**：支持将悬浮球设置为“圆角正方形”，并适配完整的动态发光与交互动效。
- **AI 配置功能**：在偏好设置中新增 AI 面板，支持自定义配置 OpenAI 兼容接口的 URL 和 API Key，为后续 AI 功能打下基础。

### 修复
- **稳定性修复**：彻底修复了修改悬浮球形状时在特定分组条件下发生的闪退问题，并解决了应用设置后悬浮球可能意外变小的 Bug。
- **笔记输入优化**：修复了在笔记列表中输入文字时，光标可能意外跳转到末尾的问题，提升了输入流畅度。

### 优化
- **删除逻辑重构**：统一了应用内各处删除悬浮球的底层实现，解决了删除球后分组成员索引未同步平移的 Bug。
- **数据一致性优化**：删除悬浮球时，同步更新所有关联分组的成员索引。个性化设置面板中的分组信息（如“分组1 (球1, 球2)”）现在能实时反映按钮删除后的最新编号。
- **新增项默认值调整**：新建悬浮球的默认大小从 68 增加至 91，且默认不再自动加入第一个分组，提供更清爽的初始体验。
- **交互联动优化**：点击悬浮球打开功能面板（如笔记、剪贴板等）时，显隐逻辑由“隐藏全局”改为“仅隐藏同分组成员”，非同组悬浮球将保持显示，支持更高效的多组协作。
- **权限引导优化**：在辅助功能引导窗口添加红色文字提醒，指导用户在权限不生效时可以通过删除并重新添加应用来修复权限。
- **形状命名规范**：优化了形状选项的描述（圆形/圆角正方形/胶囊形），并清理了个性化面板中的冗余层级。
- **拾色器优化**：去除拾色器历史列表中复制按钮的下拉箭头，保持界面简洁，同时保留多格式复制功能。
- **笔记分组优化**：笔记视图中不再显示“颜色列表”分组，实现业务逻辑解耦。
- **布局逻辑优化**：优化笔记分组区域的动态布局，当“管理分组”按钮隐藏时不再占据空间，消除界面空白行。

## v2.6.7 (2026-02-10)

### 修复
- 修复全局快捷键显隐失效问题：将监听机制从 `CGEventTap` 迁移至 Carbon `RegisterEventHotKey`，摆脱辅助功能权限依赖，提升响应性能与稳定性。

### 优化
- 增强快捷键解析能力，支持更多功能键（如方向键、ESC等）。
- 设置界面新增权限指引提示。
- **权限引导优化**：新增不可关闭的引导窗口 `PermissionGuideWindow`，支持权限自动检测与计时器自动开启，极大提升首次使用体验。
- **控制窗口优化**：右键菜单增加辅助功能权限检查，未授权时自动隐藏相关选项。
- **交互提升**：窗口绑定蒙版支持点击任意空白处退出，并更新操作指引文案。
- **引导增强**：新增应用启动时辅助功能权限自检与主动弹窗引导逻辑。

## v2.6.6 (2025-02-09)

### 文档
- 新增 AIX 功能全览文档，涵盖 16 个模块 204 个功能点

### 修复
- 修复关闭设置窗口时无条件重置休息提醒倒计时的问题
- 修复休息提醒蒙版倒计时停住问题，蒙版显示期间禁用活动检测回调
- 修复状态1到状态2的自动切换逻辑，完善休息流程

### 优化
- 全局设置面板精简为通用/提醒/关于三个Tab
- 隐藏设置项聚合至「隐藏设置」页面，通过关于页开关控制显示
- 提醒Tab辅助功能权限前置检查，聚焦时自动检测授权状态
- 全局设置样式优化：大标题与设置项字体层级区分
- 分组设置移入管理分组弹窗
- 欢迎页面改为「查看使用说明」打开外部URL
- 通用设置新增「默认浏览器」快捷入口
- 菜单栏左侧显示空闲判定时间，键鼠活动时归零，停止活动时累加
- 统一两种休息触发场景的蒙版显示，增加按钮悬停提示

## v2.6.0 (2025-02-09)

### 新功能
- 悬浮球绑定常用文件和文件夹，点击即可快速打开
- 窗口自动隐藏：监控不活跃应用，超时后自动隐藏，支持配置排除应用
- 悬浮球分组功能：同组悬浮球保持相对位置联动移动，支持水平/垂直排布
- 保存为知识时支持选择目标知识分组

### 修复
- 菜单栏计时超限：修复蒙版显示后工作时间仍累加的问题，蒙版增加三态管理
- 休息倒计时卡住：修复蒙版显示期间鼠标事件导致定时器停止的死锁问题

### 优化
- 常用文件列表支持右键「在访达中显示」
- 去除悬浮球右键菜单中的「插件」和「提醒休息」绑定项
- 网址绑定支持多浏览器选择打开
- 笔记分组选择器每行最多 5 个，选中时添加阴影效果
- 面板打开时自动调整位置，避免遮盖悬浮球
- 新建分组按钮改为悬停时滑出显示
- 面板打开时自动隐藏其他悬浮球，关闭后恢复
- 悬浮球默认颜色改为日落橙，默认大小改为 68px，增加数量上限
- 去除菜单栏「设置」齿轮图标，删除「全局设置」菜单项
- 剪切板去重后自动移至顶部
- 笔记分组创建窗口图标输入改为图标选择器
